
resource "kubernetes_pod" "test" {
  metadata {
    name = "docker-log-generator"
  }

  spec {
    container {
      image = "coffeeapplied/dockerloggenerator:1.0.2"
      name  = "docker-log-generator"
    }
  }
}
